# Task1 Evidence Archive (2026-08-19)

The untracked four-arm matrix and marker-check evidence was copied to the
external volume labeled `50528FAE528F977E`:

`tgoskits-rt-archive-20260819/task1-untracked-evidence/`

The archive contains the five result directories that were present in the
worktree before the next experiment. It includes raw logs, generated configs,
CSV data, binaries, manifests, and per-run checksums. The archive has 492
files and is verified by:

`SHA256SUMS.relative` and `VERIFY.txt`

The archive is intentionally kept outside Git because it contains repeated
AxVisor, Linux, and initramfs binaries. The compact formal summaries and
protocol files are committed in
`four-arm-matrix-final-180s-marker-20260818/`.
